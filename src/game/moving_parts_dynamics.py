import pygame
from sprites import SpriteObject
import constants as gc
import numpy as np
from player import Player


class MovingParts():
    def __init__(self, sprite_group, pivot):
        self.moving_parts = sprite_group
        self.pivot = pivot
        self.theta = 0 # Angle of the 
        
def rotateGroupAroundPoint(group):
    pass

